import { VolunteeringDomain } from './volunteering-domain';

describe('VolunteeringDomain', () => {
  it('should create an instance', () => {
    expect(new VolunteeringDomain()).toBeTruthy();
  });
});
